name: "🏗️ Build | CommonBrokerSchemas"

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  swift-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Cache SPM dependencies
        id: cache-spm
        uses: actions/cache@v4
        with:
          path: .build
          key: ${{ runner.os }}-spm-common-broker-schemas-${{ hashFiles('**/Package.resolved', '**/Package.swift') }}
          restore-keys: ${{ runner.os }}-spm-common-broker-schemas-
      - name: Swift Describe
        if: steps.cache-spm.outputs.cache-hit != 'true'
        run: swift package describe --type json
      - name: Swift Build
        run: swift build
